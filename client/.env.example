# -----------------------------------------------------------------------------
# JobHunter Client (.env.example)
# Copy to: client/.env.local
# -----------------------------------------------------------------------------

# Backend API
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Frontend feature flags / config
NEXT_PUBLIC_FEEDBACK_ENABLED=true

# Auth provider selector: keycloak | dev
# If omitted, provider is auto-detected:
# - keycloak when NEXT_PUBLIC_KEYCLOAK_URL + NEXT_PUBLIC_KEYCLOAK_REALM + NEXT_PUBLIC_KEYCLOAK_CLIENT_ID are set
# - otherwise dev
# - set to "dev" for backend /auth/dev-login flow
# - set to "keycloak" for real Keycloak login flow
# NEXT_PUBLIC_AUTH_PROVIDER=keycloak

# Keycloak (optional for local development; if omitted, app falls back to dev-login)
# NEXT_PUBLIC_KEYCLOAK_URL
# - Keycloak base URL (without /realms suffix)
# - Example: http://localhost:8080
# NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080
#
# NEXT_PUBLIC_KEYCLOAK_REALM
# - Realm name, for example: jobhunter
# NEXT_PUBLIC_KEYCLOAK_REALM=jobhunter
#
# NEXT_PUBLIC_KEYCLOAK_CLIENT_ID
# - Public frontend client id configured in the realm
# NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=jobhunter-client
#
# NEXT_PUBLIC_KEYCLOAK_SCOPE
# - Start with: openid profile email
# - Add API scopes only if backend enforces them
# NEXT_PUBLIC_KEYCLOAK_SCOPE=openid profile email
#
# NEXT_PUBLIC_KEYCLOAK_REDIRECT_URI
# - Must be allowed in Keycloak client Redirect URIs
# NEXT_PUBLIC_KEYCLOAK_REDIRECT_URI=http://localhost:3000/auth/callback
#
# NEXT_PUBLIC_KEYCLOAK_LOGOUT_REDIRECT_URI
# - Must be allowed in Keycloak client Post Logout Redirect URIs
# NEXT_PUBLIC_KEYCLOAK_LOGOUT_REDIRECT_URI=http://localhost:3000/login
