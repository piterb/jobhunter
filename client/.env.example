# -----------------------------------------------------------------------------
# JobHunter Client (.env.example)
# Copy to: client/.env.local
# -----------------------------------------------------------------------------

# Backend API
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Frontend feature flags / config
NEXT_PUBLIC_FEEDBACK_ENABLED=true

# Auth provider selector: auth0 | dev
# If omitted, provider is auto-detected:
# - auth0 when NEXT_PUBLIC_AUTH0_DOMAIN + NEXT_PUBLIC_AUTH0_CLIENT_ID are set
# - otherwise dev
# - set to "dev" for backend /auth/dev-login flow
# - set to "auth0" for real Auth0 Universal Login flow
# NEXT_PUBLIC_AUTH_PROVIDER=auth0

# Auth0 (optional for local development; if omitted, app falls back to dev-login)
# NEXT_PUBLIC_AUTH0_DOMAIN
# - Auth0 Dashboard -> Settings -> Domain
# - Example value: your-tenant.eu.auth0.com (without https://)
# NEXT_PUBLIC_AUTH0_DOMAIN=your-tenant.eu.auth0.com
#
# NEXT_PUBLIC_AUTH0_CLIENT_ID
# - Auth0 Dashboard -> Applications -> Applications -> <frontend app> -> Client ID
# NEXT_PUBLIC_AUTH0_CLIENT_ID=your-client-id
#
# NEXT_PUBLIC_AUTH0_AUDIENCE
# - Auth0 Dashboard -> Applications -> APIs -> <your API> -> Identifier
# - Must match backend OIDC_AUDIENCE
# NEXT_PUBLIC_AUTH0_AUDIENCE=https://api.jobhunter.local
#
# NEXT_PUBLIC_AUTH0_SCOPE
# - Start with: openid profile email
# - Add API scopes only if backend enforces them
# NEXT_PUBLIC_AUTH0_SCOPE=openid profile email
#
# NEXT_PUBLIC_AUTH0_REDIRECT_URI
# - Must be added in Auth0 app: Allowed Callback URLs
# - Auth0 Dashboard -> Applications -> Applications -> <frontend app> -> Settings
# NEXT_PUBLIC_AUTH0_REDIRECT_URI=http://localhost:3000/auth/callback
#
# NEXT_PUBLIC_AUTH0_LOGOUT_URL
# - Must be added in Auth0 app: Allowed Logout URLs
# - Auth0 Dashboard -> Applications -> Applications -> <frontend app> -> Settings
# NEXT_PUBLIC_AUTH0_LOGOUT_URL=http://localhost:3000/login
